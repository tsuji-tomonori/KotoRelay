# KotoRelay（コトリレー）

書いた知識を、承認を経て人とAIへ引き継ぐ文書管理RAGアプリ。

Astro / React / TypeScript、FastAPI / Python / uv、AWS CDK Python。クラウドはAurora DSQL、ローカルはPostgreSQL。要件正本は `spec/requirements/requirements.qnt`、実装の設計判断は `docs/decisions/IMPLEMENTATION.md`。

実装・検証中。実行結果と起動手順は実装に合わせて更新する。
